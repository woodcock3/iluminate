---
layout: base.liquid
---

<main class="content" id="main-content">
  <section id="{{ tag }}" class="content-section">
    <article>
      <div class="container">
        {%- if page.title %}<h1>{{ page.title | upcase }}</h1>{% endif %}
        {{ page.content }}
      </div>
    </article>
  </section>
</main>
