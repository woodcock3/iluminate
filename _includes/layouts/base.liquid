<!doctype html>
<html lang="en">
  <head>{% assign buildversion = "now" | date:'?v=%s' %}
    <meta charset="utf-8">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; img-src https://*; child-src 'none'; script-src cdn.skypack.dev">
    <link rel="stylesheet" href="{{ '/assets/css/windi.css' | append: buildversion }}">
    <script type="module" crossorigin src="{{ 'assets/js/site.js' | append: buildversion }}"></script>
    <title>{{ title }}</title>
  </head>
  <body>
    <main>
      {{ content }}
    </main>
  </body>
</html>
