<!doctype html>
<html lang="en">
  <head>{% assign buildversion = "now" | date:'?v=%s' %}
    <meta charset="utf-8">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; img-src https://*; child-src 'none';">
    <link rel="stylesheet" href="{{ '/assest/css/windi.css' | append: buildversion }}">
    <script src="{{ '/site.min.js' | append: buildversion }}"></script>
    <title>{{ title }}</title>
  </head>
  <body>
    <main>
      {{ content }}
    </main>
  </body>
</html>
